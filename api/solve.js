const GEMINI_MODELS=['gemini-3.8-flash','gemini-3.7-flash','gemini-3.6-flash'];
const GEMINI_LAST_RESORT='gemini-3.5-flash-lite';
const MAX_OUTPUT_TOKENS=12000;
const VERIFY_RETRIES=2;
function cleanKey(v){return String(v||'').replace(/^['"`]+|['"`]+$/g,'').trim()}
function json(res,status,payload){res.status(status).setHeader('Content-Type','application/json; charset=utf-8');return res.end(JSON.stringify(payload))}
function getBody(req){const b=req?.body;if(b&&typeof b==='object'&&!Array.isArray(b))return b;if(typeof b==='string'){try{const p=JSON.parse(b);if(p&&typeof p==='object')return p}catch(_){}}return {}}
function subjectMode(subject,text){const s=(String(subject||'')+' '+String(text||'')).toLowerCase();if(/toán|math|algebra|calculus|đạo hàm|tích phân|hình học|phương trình|bất đẳng thức|xác suất/.test(s))return 'math';if(/vật lý|physics|cơ học|điện|quang|dao động|sóng|nhiệt/.test(s))return 'physics';if(/hóa|chemistry|phản ứng|mol|acid|base|oxi hóa|hữu cơ/.test(s))return 'chemistry';return 'general'}
function transient(status,msg){const m=String(msg||'').toLowerCase();return status===408||status===409||status===425||status===429||status>=500||m.includes('high demand')||m.includes('resource exhausted')||m.includes('rate limit')}
function providerError(status,msg){if(status===401||status===403)return 'API AI chưa được cấp quyền hoặc khóa API không hợp lệ.';if(transient(status,msg))return 'AI đang bận tạm thời.';return 'Bộ giải AI không phản hồi hợp lệ.'}
async function sleep(ms){return new Promise(r=>setTimeout(r,ms))}
async function wolfram(query){const appid=cleanKey(process.env.WOLFRAM_APP_ID);if(!appid||!query)return {available:false};try{const r=await fetch('https://api.wolframalpha.com/v1/result?appid='+encodeURIComponent(appid)+'&i='+encodeURIComponent(query)+'&units=metric',{signal:AbortSignal.timeout(6000)});const t=await r.text();return r.ok&&t.trim()?{available:true,result:t.trim()}:{available:false}}catch{return {available:false}}}
function solverPrompt(subject,mode,verified,message,history){return `Bạn là STUDY TH — bộ giải bài học tập chuyên sâu.\nMôn: ${subject||'chưa chọn'}\nChế độ: ${mode}\n\nMỤC TIÊU: tạo lời giải có thể kiểm định, không chỉ đoán đáp án.\n\nBẮT BUỘC:\n1. Nếu có ảnh: đọc toàn bộ đề và tất cả phần nhìn thấy trước khi giải.\n2. Nếu nhiều câu: giải hết từ câu đầu đến câu cuối, không dừng giữa chừng.\n3. Giải tận gốc: Dữ kiện → Cần tìm → Ý tưởng → biến đổi/chứng minh từng bước → trường hợp đặc biệt → kiểm tra → kết luận.\n4. Với mỗi phép biến đổi có nguy cơ mất/thêm nghiệm, nêu điều kiện và kiểm tra lại.\n5. Với hàm số/phương trình/hệ/bất đẳng thức: thế ngược, kiểm tra điều kiện và tìm phản ví dụ phù hợp.\n6. Không bịa phần ảnh mờ hoặc dữ kiện thiếu.\n7. Công thức toán dùng LaTeX.\n8. Cuối lời giải ghi rõ phần đã kiểm tra.\n${verified?'\nCAS/WOLFRAM THAM KHẢO:\n'+verified+'\nChỉ dùng nếu thật sự liên quan; nếu mâu thuẫn phải kiểm tra lại.':''}\n\nLỊCH SỬ:\n${Array.isArray(history)?history.slice(-6).map(x=>(x.role||'user')+': '+String(x.message||'')).join('\n'):''}\n\nĐỀ/YÊU CẦU:\n${message}`}
async function geminiOnce({message,subject,history,imageDataUrl,verified,model,maxOutputTokens=MAX_OUTPUT_TOKENS,timeout=25000}){const key=cleanKey(process.env.GEMINI_API_KEY);if(!key)throw new Error('GEMINI_API_KEY chưa được cấu hình.');const mode=subjectMode(subject,message);const parts=[{text:solverPrompt(subject,mode,verified,message,history)}];if(imageDataUrl&&/^data:image\//i.test(imageDataUrl)){const m=imageDataUrl.match(/^data:(image\/[\w.+-]+);base64,(.+)$/s);if(m)parts.push({inlineData:{mimeType:m[1],data:m[2]}})}for(let attempt=0;attempt<2;attempt++){try{const r=await fetch('https://generativelanguage.googleapis.com/v1beta/models/'+model+':generateContent',{method:'POST',headers:{'Content-Type':'application/json','x-goog-api-key':key},body:JSON.stringify({contents:[{role:'user',parts}],generationConfig:{maxOutputTokens}}),signal:AbortSignal.timeout(timeout)});const raw=await r.text();let d={};try{d=raw?JSON.parse(raw):{}}catch{}if(r.ok){const c=d?.candidates?.[0];const answer=c?.content?.parts?.map(p=>p.text||'').join('').trim();if(answer)return {answer,model,finishReason:c?.finishReason||null}}const err=Object.assign(new Error(providerError(r.status,d?.error?.message)),{status:r.status,providerMessage:d?.error?.message});if(attempt===0&&transient(r.status,d?.error?.message)){await sleep(250);continue}throw err}catch(e){if(attempt===0&&transient(e?.status,e?.providerMessage||e?.message)){await sleep(250);continue}throw e}}throw new Error('Gemini không phản hồi.')}
async function independentSolves(args){const models=[...GEMINI_MODELS];const settled=await Promise.allSettled(models.map(model=>geminiOnce({...args,model})));let good=settled.filter(x=>x.status==='fulfilled').map(x=>x.value);if(good.length>=2)return good;if(good.length===1){try{const extra=await geminiOnce({...args,model:GEMINI_LAST_RESORT,timeout:16000});good.push(extra)}catch(_){ }return good}throw settled.find(x=>x.status==='rejected')?.reason||new Error('Gemini không phản hồi.')}
function parseJson(text){const raw=String(text||'').trim().replace(/^```json\s*/i,'').replace(/```$/,'').trim();try{return JSON.parse(raw)}catch{const m=raw.match(/\{[\s\S]*\}$/);if(m)try{return JSON.parse(m[0])}catch{}return null}}
async function verifySolution({message,imageDataUrl,verified,candidates}){const key=cleanKey(process.env.GEMINI_API_KEY);if(!key||!candidates.length)return null;const joined=candidates.map((x,i)=>`=== LỜI GIẢI ${i+1} (${x.model}) ===\n${x.answer}`).join('\n\n');const prompt=`Bạn là VERIFICATION ENGINE của STUDY TH. Kiểm tra từ đầu đến cuối, không chấp nhận “có vẻ đúng”.\n\nPHẢI LÀM:\nA) Đọc lại đề/ảnh và miền điều kiện.\nB) So sánh lời giải độc lập, tìm điểm mâu thuẫn.\nC) Lập test phù hợp: thế ngược vào đề gốc, giá trị mẫu hợp lệ, biên/đặc biệt, dấu/đơn vị và cố ý tìm phản ví dụ.\nD) Kiểm tra biến đổi tương đương và mất/thêm nghiệm.\nE) Với chứng minh, kiểm tra logic từng mệnh đề.\nF) Chỉ PASS khi các kiểm tra quan trọng đều qua; chỉ 1 lỗi toán học là FAIL.\nG) FAIL phải nêu lỗi và repair_hint.\n\nTRẢ JSON DUY NHẤT:\n{"verdict":"PASS|FAIL|UNCERTAIN","issues":["..."],"tests":[{"test":"...","result":"PASS|FAIL|NOT_APPLICABLE","note":"..."}],"repair_hint":"...","preferred":0}\n\nĐỀ:\n${message}\n\nCAS/WOLFRAM:\n${verified||'Không có'}\n\n${joined}`;const parts=[{text:prompt}];if(imageDataUrl&&/^data:image\//i.test(imageDataUrl)){const m=imageDataUrl.match(/^data:(image\/[\w.+-]+);base64,(.+)$/s);if(m)parts.push({inlineData:{mimeType:m[1],data:m[2]}})}try{const r=await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-3.6-flash:generateContent',{method:'POST',headers:{'Content-Type':'application/json','x-goog-api-key':key},body:JSON.stringify({contents:[{role:'user',parts}],generationConfig:{maxOutputTokens:4500,responseMimeType:'application/json'}}),signal:AbortSignal.timeout(18000)});const raw=await r.text();let d={};try{d=raw?JSON.parse(raw):{}}catch{}if(!r.ok)return null;const txt=d?.candidates?.[0]?.content?.parts?.map(p=>p.text||'').join('').trim();const parsed=parseJson(txt);return parsed?{...parsed,model:'gemini-3.6-flash'}:null}catch{return null}}
async function repairSolution({message,subject,imageDataUrl,verified,candidates,audit,attempt}){const i=Number.isInteger(audit?.preferred)?audit.preferred:0;const base=candidates[Math.max(0,Math.min(i,candidates.length-1))];if(!base)return null;return geminiOnce({message:`REPAIR ENGINE STUDY TH. Lời giải đã FAIL kiểm định. Giải lại TOÀN BỘ từ gốc, sửa mọi lỗi được nêu, tự kiểm tra lại và xuất lời giải hoàn chỉnh.\n\nĐỀ:\n${message}\n\nLỜI GIẢI FAIL:\n${base.answer}\n\nKIỂM ĐỊNH:\n${JSON.stringify(audit||{})}\n\nCAS/WOLFRAM:\n${verified||'Không có'}\n\nLần sửa: ${attempt}`,subject,history:[],imageDataUrl,verified:null,model:'gemini-3.8-flash',maxOutputTokens:MAX_OUTPUT_TOKENS,timeout:30000})}
async function openaiFallback({message,subject,imageDataUrl,verified}){const key=cleanKey(process.env.OPENAI_API_KEY);if(!key)return null;const model=cleanKey(process.env.OPENAI_SOLVER_MODEL||'gpt-5');const prompt=`Giải toàn bộ bài từ gốc, không chỉ kết luận. Môn: ${subject||'chưa chọn'}. Kiểm tra điều kiện, thế ngược, trường hợp đặc biệt và phản ví dụ. ${verified?'Đối chiếu CAS/Wolfram: '+verified:''}\n\n${message}`;const content=[{type:'input_text',text:prompt}];if(imageDataUrl&&/^data:image\//i.test(imageDataUrl))content.push({type:'input_image',image_url:imageDataUrl,detail:'high'});const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+key},body:JSON.stringify({model,reasoning:{effort:'high'},input:[{role:'user',content}],max_output_tokens:MAX_OUTPUT_TOKENS}),signal:AbortSignal.timeout(40000)});const raw=await r.text();let d={};try{d=raw?JSON.parse(raw):{}}catch{}if(!r.ok)throw new Error(d?.error?.message||('OpenAI HTTP '+r.status));return {answer:d.output_text||'Mình chưa có câu trả lời.',model}}
module.exports=async function handler(req,res){if(req.method!=='POST')return json(res,405,{error:'Method not allowed'});const b=getBody(req);try{const message=String(b.message||'').trim();if(!message)return json(res,400,{error:'Thiếu câu hỏi hoặc ảnh.'});const subject=String(b.subject||'');const history=Array.isArray(b.history)?b.history:[];const imageDataUrl=String(b.imageDataUrl||'');const deep=b.deep!==false;const mode=subjectMode(subject,message);const verificationPromise=(mode==='math'||mode==='physics'||mode==='chemistry')?wolfram(message):Promise.resolve({available:false});let candidates;try{candidates=await independentSolves({message,subject,history,imageDataUrl,verified:null})}catch(primaryError){let verified=null;try{const w=await verificationPromise;verified=w.available?w.result:null}catch(_e){}try{const fallback=await openaiFallback({message,subject,imageDataUrl,verified});if(fallback)return json(res,200,{...fallback,tool:verified?'WolframAlpha + OpenAI':'OpenAI fallback'})}catch(_e){}return json(res,503,{error:'AI đang bận hoặc chưa phản hồi. Hãy thử lại sau vài giây.'})}let verified=null;try{const w=await verificationPromise;verified=w.available?w.result:null}catch(_e){}let audit=null;let chosen=candidates[0];if(deep&&(mode==='math'||mode==='physics'||mode==='chemistry')){audit=await verifySolution({message,imageDataUrl,verified,candidates});for(let attempt=1;attempt<=VERIFY_RETRIES&&audit?.verdict==='FAIL';attempt++){const repaired=await repairSolution({message,subject,imageDataUrl,verified,candidates,audit,attempt}).catch(()=>null);if(!repaired)break;candidates=[repaired];chosen=repaired;audit=await verifySolution({message,imageDataUrl,verified,candidates}).catch(()=>null);if(audit?.verdict==='PASS')break}if(audit?.verdict!=='PASS'&&audit){return json(res,409,{error:'Lời giải chưa vượt qua kiểm định toán học. Hệ thống đã phát hiện điểm cần sửa; hãy gửi lại để chạy một vòng kiểm tra mới.',auditVerdict:audit.verdict,auditIssues:audit.issues||[],tests:audit.tests||[]})}if(audit?.verdict==='PASS'){const i=Number.isInteger(audit.preferred)?audit.preferred:0;chosen=candidates[Math.max(0,Math.min(i,candidates.length-1))]||chosen}}
const tools=[];if(verified)tools.push('WolframAlpha');if(candidates.length>1)tools.push(candidates.length+' AI độc lập');if(audit)tools.push('Verification Engine');return json(res,200,{answer:chosen.answer,model:chosen.model,auditModel:audit?.model||null,auditVerdict:audit?.verdict||'UNCERTAIN',auditIssues:audit?.issues||[],tests:audit?.tests||[],tool:tools.join(' + ')||'Gemini',verified:!!verified})}catch(e){console.error('solver handler error',e);return json(res,503,{error:'Solver tạm thời gặp lỗi. Hãy thử lại sau vài giây.'})}}
