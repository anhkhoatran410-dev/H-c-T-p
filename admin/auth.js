// Admin authentication is handled by admin/app.js.
// Keep this file intentionally passive so the login form does not receive
// two submit handlers and accidentally send two authentication requests.
// The session token is stored in sessionStorage by app.js.
