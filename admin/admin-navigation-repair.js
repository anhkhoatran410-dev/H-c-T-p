/* Retired navigation repair layer. The Admin now uses the core navigation from login-fix.js/app.js. */
(function(){
  'use strict';
  /* Intentionally no listeners/observers here: duplicate capture handlers and MutationObservers
     caused sluggish navigation and competed with the primary Admin router. */
})();
